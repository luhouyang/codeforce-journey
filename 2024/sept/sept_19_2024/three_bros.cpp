#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int max = 6;

    int a, b = 0;

    cin >> a >> b;

    cout << (max - a - b);
}

// 31 ms, 0 KB